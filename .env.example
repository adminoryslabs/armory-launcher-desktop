# URL base de registry-api. En local, con "npm run dev" en registry-api,
# el default ya apunta acá; solo hace falta este archivo para producción
# o para apuntar a otro backend.
VITE_API_BASE_URL=http://localhost:3000
